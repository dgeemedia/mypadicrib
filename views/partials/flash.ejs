<!-- views/partials/flash.ejs -->
<% if (success && success.length) { %>
  <div class="flash success"><%= success.join('<br>') %></div>
<% } %>
<% if (error && error.length) { %>
  <div class="flash error"><%= error.join('<br>') %></div>
<% } %>
